# Basic Crossword PDF generator and solver
Continuously generates a crossword PDF as it's being solved.

## Example usage:
`cargo run -r -- -p "examples/Newsday - 20250429 - 42925 SECURITY NUMBERS.puz"`
Run with `--help` to see more info.
